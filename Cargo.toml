[package]
name = "pack_create"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.6.1", features = ["derive"] }
csv = "1.4.0"
rand = "0.10.1"
serde = { version = "1.0.228", features = ["derive"] }

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
